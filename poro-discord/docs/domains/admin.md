# 운영/관리자 도메인 — 명세 (스텁, 설계 선행)

> **[도메인: 운영]** **[STATUS: STUB/설계 선행]** — 보안 영향이 큰 영역.
> 실제 구현 전 설계/승인을 선행한다. 코드: `modules/admin/commands.py`(빈 Cog).

## 보안 원칙

- 모든 운영 명령어는 `core/permissions.py` 의 `@requires_permission(...)` 로 보호한다.
- 운영 권한 역할은 **수동 지급 전용**(봇 자동 지급 금지) — [`../roles_and_permissions.md`] §B.
- 상태 변경(역할 부여·골드 지급 등)은 게임 서버 API 경유로만, 사용자가 명시적으로
  요청할 때만 구현한다.
- 운영 명령어는 전용 채널(`#운영로그`)에서만 실행하도록 제한하는 방향을 검토한다.

## 명령어 후보 (TODO)

| 명령어 | 기능 | 권한 |
|---|---|---|
| `/공지 <내용>` | 공식 공지 게시 | admin |
| `/점검 <시작> <예상시간>` | 점검 안내 + `@점검알림` 멘션 | admin |
| `/보스알림 <보스> <채널>` | 수동 보스 등장 알림 | admin / event_manager |
| `/역할부여 <유저> <역할>` | 수동 역할 부여(테스터 등) — 권한 역할 부여는 운영진 한정 | admin |
| `/유저조회 <닉네임>` | 플레이어 상세 조회 (ephemeral) | admin |
| `/골드지급 <닉네임> <금액> <사유>` | 골드 지급(API 경유 + 운영로그 기록) | rpg_manager |
| `/이벤트등록 <날짜> <내용>` | 이벤트 일정 등록/수정/삭제 | event_manager |
| `/닉네임변경 <유저> <새닉네임> <사유>` | 연동 닉네임 변경(다른 마크 계정 전환) — 운영로그 기록 | admin |
| `/닉네임재입력 <유저>` (토글) | 해당 유저의 닉네임 재입력/재인증 허용 토글 | admin |

### 닉네임 변경 정책 (DL-132)

- 닉네임은 공통 인증 시 1회 입력하며(DL-131), **플레이어 셀프 변경은 금지**한다.
- 다른 마인크래프트 계정으로 플레이하려는 유저는 **운영자에게 문의** → 운영자가
  `/닉네임변경`(직접 교체) 또는 `/닉네임재입력`(유저 본인 재입력 허용 토글)으로 처리한다.
- 닉네임 교체는 기존 서버 화이트리스트 정합(구 닉네임 해제 / 신 닉네임 재인증)을 동반하므로
  **반드시 게임 서버 API 경유 + 운영로그 기록**한다. 실구현은 사용자 명시 요청 시.

> 위 명령어의 RPG 측 상세(임베드 형식·예외 처리 등)는 [`rpg.md`](rpg.md) §3-2·§4 참조.
> 본 문서는 중앙제어 권한/보안 관점의 정리다.
